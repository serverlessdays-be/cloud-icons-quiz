import React from "react";

const Container = ({ children }) => (
  <div
    style={{
      margin: "0 auto",
      padding: "50px 100px",
    }}
  >
    {children}
  </div>
);

export default Container;
